[project]
name = "vtn-notes"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["vtn-core", "vtn-style"]

[tool.uv.sources]
vtn-core = { workspace = true }
vtn-style = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
