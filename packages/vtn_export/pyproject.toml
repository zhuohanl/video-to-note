[project]
name = "vtn-export"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["vtn-core", "vtn-notes"]

[tool.uv.sources]
vtn-core = { workspace = true }
vtn-notes = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
