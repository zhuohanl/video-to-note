[project]
name = "vtn-visual"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["vtn-core", "imagehash>=4.3.0", "imageio-ffmpeg>=0.6.0", "pillow>=12.0.0"]

[tool.uv.sources]
vtn-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
